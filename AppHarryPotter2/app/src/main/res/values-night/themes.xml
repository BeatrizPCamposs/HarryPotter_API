<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppHarryPotter" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Dourado</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Light mode com Shared Preferences -->

    <!-- Screen background if Light mode on -->
    <style name="screen">
        <item name="android:background">#fff</item>
    </style>

    <style name="texto">
        <item name="android:textColor">#000</item>
    </style>

    <style name="textoo">
        <item name="android:textColorHint">#000</item>
    </style>

</resources>